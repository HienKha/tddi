# Rename this file to `.env` and fill in the secrets before running the app locally
# or deploying it. Never commit the populated `.env` file to version control.

# === Gemini configuration ===
GEMINI_API_KEY=your-gemini-api-key
# Optional overrides
# GEMINI_MODEL=gemini-2.5-pro
# GEMINI_TOP_FEATURES=15
